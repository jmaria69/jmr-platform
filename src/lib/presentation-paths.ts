/**
 * Proyectos con landing de presentación propia dentro del sitio — antes de
 * saltar a la app externa, el visitante (y la captura de pantalla) pasan
 * primero por aquí. Debe reflejar `CON_LANDING` en `src/lib/laboratorio.ts`.
 */
export const PRESENTATION_PATHS: Record<string, string> = {
  siam: "/siam",
  "admin-app": "/adminapp",
  "crm-it": "/core-ops",
};
